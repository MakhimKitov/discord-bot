# Utility Bot — PRD (v1)

> **Status: baseline requirements + stack approved by owner (2026-07-02).** Codex spec
> review still pending before the PRD is fully authoritative.
>
> Migrated from the legacy `ops/tasks/T0001` task during the move to the GitHub-native
> flow.

## Product decision (owner, 2026-06-24)

- A **simple utility Discord bot**, built from a **minimal baseline**.
- Keep the baseline deliberately small: the point of the experiment is the **maintenance
  loop** driven by the autonomous developer, so most of the work should happen *after*
  v1 lands, not in it.

## Tech stack (owner, 2026-07-02)

**Python 3.12 + `discord.py`**, `uv` for env/deps.

- The owner reads Python (Go was the alternative).
- Planned maintenance tasks include **audio features** (see backlog): `discord.py` has
  first-class voice support and the `yt-dlp` + FFmpeg path is the best-documented route
  to them. In Go, voice means hand-rolling Opus frame handling — the maintenance tasks
  would measure stack friction instead of the developer.
- Slash commands only (`app_commands`); no message-content intent — the bot never reads
  free-form user messages, which also narrows the untrusted-input surface (rule 3).

## Requirements (v1 baseline)

Functional — three utility slash commands, enough to establish the command pattern:

- **`/ping`** — round-trip latency check; replies with gateway latency in ms.
- **`/roll [dice]`** — roll dice in `NdM` notation (default `1d6`, e.g. `2d20`);
  validates input, replies with the rolls and total. See the maintenance log (#7)
  for the current bounds and reply format.
- **`/choose <options>`** — pick one option from a comma-separated list. An option may
  end in `:N` to weight it (`pizza:3, sushi` picks pizza 3× as often); unweighted
  options default to weight 1. See the maintenance log (#9) for the weight-recognition
  rule and bounds.

Non-functional:

- **Config via environment only**: `DISCORD_TOKEN` (required), `GUILD_ID` (optional —
  instant command sync to one guild during development). `.env` supported locally,
  never committed.
- **Logging** to stdout (the platform's monitor will consume it in phase 1).
- **Errors**: user-facing failures reply with a short message; command logic is pure
  functions, unit-tested without network.
- **CI**: commitlint + pytest on every PR (`tests` job).

## Backlog (maintenance loop — NOT baseline)

Worked by the autonomous developer through `human` issues once the loop runs:

- **Play music from YouTube** (`/play`, voice-channel audio via `discord.py[voice]` +
  `yt-dlp` + FFmpeg) — the planned capability-test task.
- Further utility commands, error-handling polish, operational hardening — as filed.

## Maintenance log

Chance/game commands added after the v1 baseline, each following the pure-logic +
thin-wrapper + `register` pattern established above:

- **`/casino`** (#3) — spins a three-reel one-armed bandit. Draws three symbols
  independently from a fixed 5-symbol set (`🍒 🍋 🔔 ⭐ 💎`) and replies
  with the reels plus one outcome tier: **jackpot** (three of a kind), **small win**
  (exactly two of a kind), or **bust** (all different). No bets, currency, balances,
  persistence, or per-user state — payouts remain a possible follow-up, out of scope.
- **Weighted reels** (#5) — reel draws are no longer uniform. Each reel draws
  independently from a fixed weight table (`CASINO_WEIGHTS`, strictly descending from
  `🍒` to `💎`), so common symbols dominate and a `💎` jackpot is a genuinely rare
  event. Outcome tiers and reply format are unchanged from #3.
- **`/roll` tightened to plain `NdM`** (#7) — parsing is now strict: both the dice
  count and the side count must be present as digits (`d20` is rejected, not read as
  `1d20`); the command's own `1d6` default only applies when the `dice` option is
  omitted entirely. Bounds are now **1–20 dice** (was 1–100) and **2–1000 sides**
  (unchanged), chosen so the reply is provably short — worst case (`20d1000`, every
  die maxed) is 157 characters, far under Discord's 2000-character limit. Reply format
  changed to `🎲 2d6 → 4 + 6 = 10`, dropping the sum for a single die (`🎲 1d6 → 5`).
  Modifiers (`2d6+3`), multiple dice groups, and advantage/disadvantage remain
  explicitly out of scope.
- **Weighted `/choose`** (#9) — options may carry an optional trailing `:N` integer
  weight (`pizza:3, sushi` picks pizza 3× as often as sushi); an option without a
  suffix keeps weight 1, and weighted/unweighted options mix freely. Recognition rule:
  split each option on its *last* colon — if the trimmed remainder is pure digits it's
  a weight (`pizza: 3` included, spaces tolerated), otherwise the colon is literal text
  (`red:ish`, `pizza:2.5`, `ratio 3:x` are unchanged literal options). A pure-digit
  weight must be 1–100; an out-of-range weight (`pizza:0`, `pizza:101`) or one attached
  to empty option text (`:3`) rejects the whole command with an ephemeral message
  naming the valid form and bounds. The reply never leaks the weight suffix
  (`I choose **pizza**`, not `I choose **pizza:3**`).

## Open items

- [x] Draft the functional requirements + acceptance criteria.
- [x] Owner resolves open decisions (stack) and approves the baseline.
- [ ] Codex spec review run; findings addressed or owner-accepted.
- [ ] Mark PRD authoritative.
