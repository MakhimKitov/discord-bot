# Copy to .env and fill in. Never commit .env (see .gitignore).
DISCORD_TOKEN=

# Optional: a dev guild id — slash commands sync there instantly instead of
# taking up to an hour to propagate globally.
GUILD_ID=
